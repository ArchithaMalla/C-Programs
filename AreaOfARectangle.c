#include<stdio.h>
main()
{
float area,lenght,width;
printf("the lenght of the rectangle in cm is:");
scanf("%f",&lenght);
printf("the width of the rectangle in cm is:");
scanf("%f",&width);
area=lenght*width;
printf("The area of the rectangle is %f cm^2",area);
}
